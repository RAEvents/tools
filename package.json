{
  "name": "tools",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm esbuild src/app.js --bundle --sourcemap --outdir=dist --define:DEBUG=true --watch --servedir=dist",
    "prod": "pnpm esbuild src/app.js --bundle --minify --outdir=dist --define:DEBUG=false"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@retroachievements/api": "^2.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
